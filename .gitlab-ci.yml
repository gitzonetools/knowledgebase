image: hosttoday/ht-docker-node:npmci

pages:
  script:
  - npmci command npm install -g gitbook-cli
  - npmci command gitbook install ./gitbook
  - npmci command gitbook build ./gitbook ./public
  artifacts:
    expire_in: 1 week
    paths:
    - public
